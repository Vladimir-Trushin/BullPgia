#pragma once

#include <string>
using std::string;

////----------------------------------------------------------------------
// Check the matching the digits
string calculateBullAndPgia(string choice, string guess);